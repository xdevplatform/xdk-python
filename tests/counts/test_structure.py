# AUTO-GENERATED FILE - DO NOT EDIT
# This file was automatically generated by the XDK build tool.
# Any manual changes will be overwritten on the next generation.
"""
Auto-generated structural tests for {"class_name": "Counts", "display_name": "counts", "import_name": "counts", "original": ["counts"], "property_name": "counts"} client.

This module contains tests that validate the structure and API surface
of the {"class_name": "Counts", "display_name": "counts", "import_name": "counts", "original": ["counts"], "property_name": "counts"} client. These tests ensure that all expected methods
exist, have correct signatures, and proper type annotations for robust API contracts.

Generated automatically - do not edit manually.
"""

import pytest
import inspect
from typing import get_type_hints
from xdk.counts.client import CountsClient
from xdk import Client


class TestCountsStructure:
    """Test the structure of CountsClient."""


    def setup_class(self):
        """Set up test fixtures."""
        # Provide all authentication types for comprehensive test coverage
        # Tests mock the session, so actual HTTP requests won't be made
        from xdk.oauth1_auth import OAuth1
        oauth1 = OAuth1(
            api_key="test_api_key",
            api_secret="test_api_secret",
            callback="http://localhost:8080/callback",
            access_token="test_access_token",
            access_token_secret="test_access_token_secret",
        )
        self.client = Client(
            base_url="https://api.example.com",
            bearer_token="test_bearer_token",
            access_token="test_access_token",
            auth=oauth1,
        )
        self.counts_client = getattr(self.client, "counts")


    def test_get_posts_recent_exists(self):
        """Test that get_posts_recent method exists with correct signature."""
        # Check method exists
        method = getattr(CountsClient, "get_posts_recent", None)
        assert (
            method is not None
        ), f"Method get_posts_recent does not exist on CountsClient"
        # Check method is callable
        assert callable(method), f"get_posts_recent is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert (
            len(params) >= 1
        ), f"get_posts_recent should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = [
            "query",
        ]
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from get_posts_recent"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = [
            "start_time",
            "end_time",
            "since_id",
            "until_id",
            "next_token",
            "pagination_token",
            "granularity",
            "search_count.fields",
        ]
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_get_posts_recent_return_annotation(self):
        """Test that get_posts_recent has proper return type annotation."""
        method = getattr(CountsClient, "get_posts_recent")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method get_posts_recent should have return type annotation"


    def test_get_posts_all_exists(self):
        """Test that get_posts_all method exists with correct signature."""
        # Check method exists
        method = getattr(CountsClient, "get_posts_all", None)
        assert (
            method is not None
        ), f"Method get_posts_all does not exist on CountsClient"
        # Check method is callable
        assert callable(method), f"get_posts_all is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert len(params) >= 1, f"get_posts_all should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = [
            "query",
        ]
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from get_posts_all"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = [
            "start_time",
            "end_time",
            "since_id",
            "until_id",
            "next_token",
            "pagination_token",
            "granularity",
            "search_count.fields",
        ]
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_get_posts_all_return_annotation(self):
        """Test that get_posts_all has proper return type annotation."""
        method = getattr(CountsClient, "get_posts_all")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method get_posts_all should have return type annotation"


    def test_all_expected_methods_exist(self):
        """Test that all expected methods exist on the client."""
        expected_methods = [
            "get_posts_recent",
            "get_posts_all",
        ]
        for expected_method in expected_methods:
            assert hasattr(
                CountsClient, expected_method
            ), f"Expected method '{expected_method}' not found on CountsClient"
            assert callable(
                getattr(CountsClient, expected_method)
            ), f"'{expected_method}' exists but is not callable"

# AUTO-GENERATED FILE - DO NOT EDIT
# This file was automatically generated by the XDK build tool.
# Any manual changes will be overwritten on the next generation.
"""
Auto-generated structural tests for {"class_name": "Broadcasts", "display_name": "broadcasts", "import_name": "broadcasts", "original": ["broadcasts"], "property_name": "broadcasts"} client.

This module contains tests that validate the structure and API surface
of the {"class_name": "Broadcasts", "display_name": "broadcasts", "import_name": "broadcasts", "original": ["broadcasts"], "property_name": "broadcasts"} client. These tests ensure that all expected methods
exist, have correct signatures, and proper type annotations for robust API contracts.

Generated automatically - do not edit manually.
"""

import pytest
import inspect
from typing import get_type_hints
from xdk.broadcasts.client import BroadcastsClient
from xdk import Client


class TestBroadcastsStructure:
    """Test the structure of BroadcastsClient."""


    def setup_class(self):
        """Set up test fixtures."""
        # Provide all authentication types for comprehensive test coverage
        # Tests mock the session, so actual HTTP requests won't be made
        from xdk.oauth1_auth import OAuth1
        oauth1 = OAuth1(
            api_key="test_api_key",
            api_secret="test_api_secret",
            callback="http://localhost:8080/callback",
            access_token="test_access_token",
            access_token_secret="test_access_token_secret",
        )
        self.client = Client(
            base_url="https://api.example.com",
            bearer_token="test_bearer_token",
            access_token="test_access_token",
            auth=oauth1,
        )
        self.broadcasts_client = getattr(self.client, "broadcasts")


    def test_get_scheduled_broadcast_exists(self):
        """Test that get_scheduled_broadcast method exists with correct signature."""
        # Check method exists
        method = getattr(BroadcastsClient, "get_scheduled_broadcast", None)
        assert (
            method is not None
        ), f"Method get_scheduled_broadcast does not exist on BroadcastsClient"
        # Check method is callable
        assert callable(method), f"get_scheduled_broadcast is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert (
            len(params) >= 1
        ), f"get_scheduled_broadcast should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = [
            "id",
        ]
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from get_scheduled_broadcast"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = []
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_get_scheduled_broadcast_return_annotation(self):
        """Test that get_scheduled_broadcast has proper return type annotation."""
        method = getattr(BroadcastsClient, "get_scheduled_broadcast")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method get_scheduled_broadcast should have return type annotation"


    def test_update_scheduled_broadcast_exists(self):
        """Test that update_scheduled_broadcast method exists with correct signature."""
        # Check method exists
        method = getattr(BroadcastsClient, "update_scheduled_broadcast", None)
        assert (
            method is not None
        ), f"Method update_scheduled_broadcast does not exist on BroadcastsClient"
        # Check method is callable
        assert callable(method), f"update_scheduled_broadcast is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert (
            len(params) >= 1
        ), f"update_scheduled_broadcast should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = [
            "id",
        ]
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from update_scheduled_broadcast"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = []
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_update_scheduled_broadcast_return_annotation(self):
        """Test that update_scheduled_broadcast has proper return type annotation."""
        method = getattr(BroadcastsClient, "update_scheduled_broadcast")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method update_scheduled_broadcast should have return type annotation"


    def test_delete_scheduled_broadcast_exists(self):
        """Test that delete_scheduled_broadcast method exists with correct signature."""
        # Check method exists
        method = getattr(BroadcastsClient, "delete_scheduled_broadcast", None)
        assert (
            method is not None
        ), f"Method delete_scheduled_broadcast does not exist on BroadcastsClient"
        # Check method is callable
        assert callable(method), f"delete_scheduled_broadcast is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert (
            len(params) >= 1
        ), f"delete_scheduled_broadcast should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = [
            "id",
        ]
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from delete_scheduled_broadcast"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = [
            "roll_forward",
        ]
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_delete_scheduled_broadcast_return_annotation(self):
        """Test that delete_scheduled_broadcast has proper return type annotation."""
        method = getattr(BroadcastsClient, "delete_scheduled_broadcast")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method delete_scheduled_broadcast should have return type annotation"


    def test_send_broadcast_chat_exists(self):
        """Test that send_broadcast_chat method exists with correct signature."""
        # Check method exists
        method = getattr(BroadcastsClient, "send_broadcast_chat", None)
        assert (
            method is not None
        ), f"Method send_broadcast_chat does not exist on BroadcastsClient"
        # Check method is callable
        assert callable(method), f"send_broadcast_chat is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert (
            len(params) >= 1
        ), f"send_broadcast_chat should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = [
            "id",
        ]
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from send_broadcast_chat"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = []
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_send_broadcast_chat_return_annotation(self):
        """Test that send_broadcast_chat has proper return type annotation."""
        method = getattr(BroadcastsClient, "send_broadcast_chat")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method send_broadcast_chat should have return type annotation"


    def test_list_scheduled_exists(self):
        """Test that list_scheduled method exists with correct signature."""
        # Check method exists
        method = getattr(BroadcastsClient, "list_scheduled", None)
        assert (
            method is not None
        ), f"Method list_scheduled does not exist on BroadcastsClient"
        # Check method is callable
        assert callable(method), f"list_scheduled is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert len(params) >= 1, f"list_scheduled should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = []
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from list_scheduled"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = [
            "max_results",
            "oldest_start_time",
            "newest_start_time",
            "pagination_token",
        ]
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_list_scheduled_return_annotation(self):
        """Test that list_scheduled has proper return type annotation."""
        method = getattr(BroadcastsClient, "list_scheduled")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method list_scheduled should have return type annotation"


    def test_list_scheduled_pagination_params(self):
        """Test that list_scheduled has pagination parameters."""
        method = getattr(BroadcastsClient, "list_scheduled")
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have pagination-related parameters
        pagination_params = [
            "pagination_token",
            "max_results",
            "next_token",
            "cursor",
            "limit",
        ]
        has_pagination_param = any(param in params for param in pagination_params)
        assert (
            has_pagination_param
        ), f"Paginated method list_scheduled should have pagination parameters"


    def test_create_scheduled_broadcast_exists(self):
        """Test that create_scheduled_broadcast method exists with correct signature."""
        # Check method exists
        method = getattr(BroadcastsClient, "create_scheduled_broadcast", None)
        assert (
            method is not None
        ), f"Method create_scheduled_broadcast does not exist on BroadcastsClient"
        # Check method is callable
        assert callable(method), f"create_scheduled_broadcast is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert (
            len(params) >= 1
        ), f"create_scheduled_broadcast should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = []
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from create_scheduled_broadcast"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = []
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_create_scheduled_broadcast_return_annotation(self):
        """Test that create_scheduled_broadcast has proper return type annotation."""
        method = getattr(BroadcastsClient, "create_scheduled_broadcast")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method create_scheduled_broadcast should have return type annotation"


    def test_go_live_scheduled_broadcast_exists(self):
        """Test that go_live_scheduled_broadcast method exists with correct signature."""
        # Check method exists
        method = getattr(BroadcastsClient, "go_live_scheduled_broadcast", None)
        assert (
            method is not None
        ), f"Method go_live_scheduled_broadcast does not exist on BroadcastsClient"
        # Check method is callable
        assert callable(method), f"go_live_scheduled_broadcast is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert (
            len(params) >= 1
        ), f"go_live_scheduled_broadcast should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = [
            "id",
        ]
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from go_live_scheduled_broadcast"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = []
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_go_live_scheduled_broadcast_return_annotation(self):
        """Test that go_live_scheduled_broadcast has proper return type annotation."""
        method = getattr(BroadcastsClient, "go_live_scheduled_broadcast")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method go_live_scheduled_broadcast should have return type annotation"


    def test_all_expected_methods_exist(self):
        """Test that all expected methods exist on the client."""
        expected_methods = [
            "get_scheduled_broadcast",
            "update_scheduled_broadcast",
            "delete_scheduled_broadcast",
            "send_broadcast_chat",
            "list_scheduled",
            "create_scheduled_broadcast",
            "go_live_scheduled_broadcast",
        ]
        for expected_method in expected_methods:
            assert hasattr(
                BroadcastsClient, expected_method
            ), f"Expected method '{expected_method}' not found on BroadcastsClient"
            assert callable(
                getattr(BroadcastsClient, expected_method)
            ), f"'{expected_method}' exists but is not callable"

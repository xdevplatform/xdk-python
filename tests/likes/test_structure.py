# AUTO-GENERATED FILE - DO NOT EDIT
# This file was automatically generated by the XDK build tool.
# Any manual changes will be overwritten on the next generation.
"""
Auto-generated structural tests for {"class_name": "Likes", "display_name": "likes", "import_name": "likes", "original": ["likes"], "property_name": "likes"} client.

This module contains tests that validate the structure and API surface
of the {"class_name": "Likes", "display_name": "likes", "import_name": "likes", "original": ["likes"], "property_name": "likes"} client. These tests ensure that all expected methods
exist, have correct signatures, and proper type annotations for robust API contracts.

Generated automatically - do not edit manually.
"""

import pytest
import inspect
from typing import get_type_hints
from xdk.likes.client import LikesClient
from xdk import Client


class TestLikesStructure:
    """Test the structure of LikesClient."""


    def setup_class(self):
        """Set up test fixtures."""
        # Provide all authentication types for comprehensive test coverage
        # Tests mock the session, so actual HTTP requests won't be made
        from xdk.oauth1_auth import OAuth1
        oauth1 = OAuth1(
            api_key="test_api_key",
            api_secret="test_api_secret",
            callback="http://localhost:8080/callback",
            access_token="test_access_token",
            access_token_secret="test_access_token_secret",
        )
        self.client = Client(
            base_url="https://api.example.com",
            bearer_token="test_bearer_token",
            access_token="test_access_token",
            auth=oauth1,
        )
        self.likes_client = getattr(self.client, "likes")


    def test_unlike_post_exists(self):
        """Test that unlike_post method exists with correct signature."""
        # Check method exists
        method = getattr(LikesClient, "unlike_post", None)
        assert method is not None, f"Method unlike_post does not exist on LikesClient"
        # Check method is callable
        assert callable(method), f"unlike_post is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert len(params) >= 1, f"unlike_post should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = [
            "id",
            "tweet_id",
        ]
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from unlike_post"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = []
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_unlike_post_return_annotation(self):
        """Test that unlike_post has proper return type annotation."""
        method = getattr(LikesClient, "unlike_post")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method unlike_post should have return type annotation"


    def test_like_post_exists(self):
        """Test that like_post method exists with correct signature."""
        # Check method exists
        method = getattr(LikesClient, "like_post", None)
        assert method is not None, f"Method like_post does not exist on LikesClient"
        # Check method is callable
        assert callable(method), f"like_post is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert len(params) >= 1, f"like_post should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = [
            "id",
        ]
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from like_post"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = []
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_like_post_return_annotation(self):
        """Test that like_post has proper return type annotation."""
        method = getattr(LikesClient, "like_post")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method like_post should have return type annotation"


    def test_get_posts_liking_users_exists(self):
        """Test that get_posts_liking_users method exists with correct signature."""
        # Check method exists
        method = getattr(LikesClient, "get_posts_liking_users", None)
        assert (
            method is not None
        ), f"Method get_posts_liking_users does not exist on LikesClient"
        # Check method is callable
        assert callable(method), f"get_posts_liking_users is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert (
            len(params) >= 1
        ), f"get_posts_liking_users should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = [
            "id",
        ]
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from get_posts_liking_users"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = [
            "max_results",
            "pagination_token",
            "user.fields",
            "expansions",
            "post.fields",
        ]
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_get_posts_liking_users_return_annotation(self):
        """Test that get_posts_liking_users has proper return type annotation."""
        method = getattr(LikesClient, "get_posts_liking_users")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method get_posts_liking_users should have return type annotation"


    def test_get_posts_liking_users_pagination_params(self):
        """Test that get_posts_liking_users has pagination parameters."""
        method = getattr(LikesClient, "get_posts_liking_users")
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have pagination-related parameters
        pagination_params = [
            "pagination_token",
            "max_results",
            "next_token",
            "cursor",
            "limit",
        ]
        has_pagination_param = any(param in params for param in pagination_params)
        assert (
            has_pagination_param
        ), f"Paginated method get_posts_liking_users should have pagination parameters"


    def test_all_expected_methods_exist(self):
        """Test that all expected methods exist on the client."""
        expected_methods = [
            "unlike_post",
            "like_post",
            "get_posts_liking_users",
        ]
        for expected_method in expected_methods:
            assert hasattr(
                LikesClient, expected_method
            ), f"Expected method '{expected_method}' not found on LikesClient"
            assert callable(
                getattr(LikesClient, expected_method)
            ), f"'{expected_method}' exists but is not callable"

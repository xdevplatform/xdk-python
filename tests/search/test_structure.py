# AUTO-GENERATED FILE - DO NOT EDIT
# This file was automatically generated by the XDK build tool.
# Any manual changes will be overwritten on the next generation.
"""
Auto-generated structural tests for {"class_name": "Search", "display_name": "search", "import_name": "search", "original": ["search"], "property_name": "search"} client.

This module contains tests that validate the structure and API surface
of the {"class_name": "Search", "display_name": "search", "import_name": "search", "original": ["search"], "property_name": "search"} client. These tests ensure that all expected methods
exist, have correct signatures, and proper type annotations for robust API contracts.

Generated automatically - do not edit manually.
"""

import pytest
import inspect
from typing import get_type_hints
from xdk.search.client import SearchClient
from xdk import Client


class TestSearchStructure:
    """Test the structure of SearchClient."""


    def setup_class(self):
        """Set up test fixtures."""
        # Provide all authentication types for comprehensive test coverage
        # Tests mock the session, so actual HTTP requests won't be made
        from xdk.oauth1_auth import OAuth1
        oauth1 = OAuth1(
            api_key="test_api_key",
            api_secret="test_api_secret",
            callback="http://localhost:8080/callback",
            access_token="test_access_token",
            access_token_secret="test_access_token_secret",
        )
        self.client = Client(
            base_url="https://api.example.com",
            bearer_token="test_bearer_token",
            access_token="test_access_token",
            auth=oauth1,
        )
        self.search_client = getattr(self.client, "search")


    def test_posts_recent_exists(self):
        """Test that posts_recent method exists with correct signature."""
        # Check method exists
        method = getattr(SearchClient, "posts_recent", None)
        assert method is not None, f"Method posts_recent does not exist on SearchClient"
        # Check method is callable
        assert callable(method), f"posts_recent is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert len(params) >= 1, f"posts_recent should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = [
            "query",
        ]
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from posts_recent"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = [
            "max_results",
            "next_token",
            "pagination_token",
            "start_time",
            "end_time",
            "since_id",
            "until_id",
            "sort_order",
            "post.fields",
            "expansions",
            "user.fields",
            "media.fields",
            "poll.fields",
            "place.fields",
        ]
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_posts_recent_return_annotation(self):
        """Test that posts_recent has proper return type annotation."""
        method = getattr(SearchClient, "posts_recent")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method posts_recent should have return type annotation"


    def test_posts_recent_pagination_params(self):
        """Test that posts_recent has pagination parameters."""
        method = getattr(SearchClient, "posts_recent")
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have pagination-related parameters
        pagination_params = [
            "pagination_token",
            "max_results",
            "next_token",
            "cursor",
            "limit",
        ]
        has_pagination_param = any(param in params for param in pagination_params)
        assert (
            has_pagination_param
        ), f"Paginated method posts_recent should have pagination parameters"


    def test_news_exists(self):
        """Test that news method exists with correct signature."""
        # Check method exists
        method = getattr(SearchClient, "news", None)
        assert method is not None, f"Method news does not exist on SearchClient"
        # Check method is callable
        assert callable(method), f"news is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert len(params) >= 1, f"news should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = [
            "query",
        ]
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from news"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = [
            "max_results",
            "max_age_hours",
            "news.fields",
        ]
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_news_return_annotation(self):
        """Test that news has proper return type annotation."""
        method = getattr(SearchClient, "news")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method news should have return type annotation"


    def test_posts_all_exists(self):
        """Test that posts_all method exists with correct signature."""
        # Check method exists
        method = getattr(SearchClient, "posts_all", None)
        assert method is not None, f"Method posts_all does not exist on SearchClient"
        # Check method is callable
        assert callable(method), f"posts_all is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert len(params) >= 1, f"posts_all should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = [
            "query",
        ]
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from posts_all"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = [
            "start_time",
            "end_time",
            "since_id",
            "until_id",
            "max_results",
            "next_token",
            "pagination_token",
            "sort_order",
            "post.fields",
            "expansions",
            "user.fields",
            "media.fields",
            "poll.fields",
            "place.fields",
        ]
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_posts_all_return_annotation(self):
        """Test that posts_all has proper return type annotation."""
        method = getattr(SearchClient, "posts_all")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method posts_all should have return type annotation"


    def test_posts_all_pagination_params(self):
        """Test that posts_all has pagination parameters."""
        method = getattr(SearchClient, "posts_all")
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have pagination-related parameters
        pagination_params = [
            "pagination_token",
            "max_results",
            "next_token",
            "cursor",
            "limit",
        ]
        has_pagination_param = any(param in params for param in pagination_params)
        assert (
            has_pagination_param
        ), f"Paginated method posts_all should have pagination parameters"


    def test_all_expected_methods_exist(self):
        """Test that all expected methods exist on the client."""
        expected_methods = [
            "posts_recent",
            "news",
            "posts_all",
        ]
        for expected_method in expected_methods:
            assert hasattr(
                SearchClient, expected_method
            ), f"Expected method '{expected_method}' not found on SearchClient"
            assert callable(
                getattr(SearchClient, expected_method)
            ), f"'{expected_method}' exists but is not callable"

# AUTO-GENERATED FILE - DO NOT EDIT
# This file was automatically generated by the XDK build tool.
# Any manual changes will be overwritten on the next generation.
"""
Auto-generated structural tests for {"class_name": "Articles", "display_name": "articles", "import_name": "articles", "original": ["articles"], "property_name": "articles"} client.

This module contains tests that validate the structure and API surface
of the {"class_name": "Articles", "display_name": "articles", "import_name": "articles", "original": ["articles"], "property_name": "articles"} client. These tests ensure that all expected methods
exist, have correct signatures, and proper type annotations for robust API contracts.

Generated automatically - do not edit manually.
"""

import pytest
import inspect
from typing import get_type_hints
from xdk.articles.client import ArticlesClient
from xdk import Client


class TestArticlesStructure:
    """Test the structure of ArticlesClient."""


    def setup_class(self):
        """Set up test fixtures."""
        # Provide all authentication types for comprehensive test coverage
        # Tests mock the session, so actual HTTP requests won't be made
        from xdk.oauth1_auth import OAuth1
        oauth1 = OAuth1(
            api_key="test_api_key",
            api_secret="test_api_secret",
            callback="http://localhost:8080/callback",
            access_token="test_access_token",
            access_token_secret="test_access_token_secret",
        )
        self.client = Client(
            base_url="https://api.example.com",
            bearer_token="test_bearer_token",
            access_token="test_access_token",
            auth=oauth1,
        )
        self.articles_client = getattr(self.client, "articles")


    def test_article_publish_exists(self):
        """Test that article_publish method exists with correct signature."""
        # Check method exists
        method = getattr(ArticlesClient, "article_publish", None)
        assert (
            method is not None
        ), f"Method article_publish does not exist on ArticlesClient"
        # Check method is callable
        assert callable(method), f"article_publish is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert (
            len(params) >= 1
        ), f"article_publish should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = [
            "article_id",
        ]
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from article_publish"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = []
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_article_publish_return_annotation(self):
        """Test that article_publish has proper return type annotation."""
        method = getattr(ArticlesClient, "article_publish")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method article_publish should have return type annotation"


    def test_article_create_draft_exists(self):
        """Test that article_create_draft method exists with correct signature."""
        # Check method exists
        method = getattr(ArticlesClient, "article_create_draft", None)
        assert (
            method is not None
        ), f"Method article_create_draft does not exist on ArticlesClient"
        # Check method is callable
        assert callable(method), f"article_create_draft is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert (
            len(params) >= 1
        ), f"article_create_draft should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = []
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from article_create_draft"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = []
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_article_create_draft_return_annotation(self):
        """Test that article_create_draft has proper return type annotation."""
        method = getattr(ArticlesClient, "article_create_draft")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method article_create_draft should have return type annotation"


    def test_all_expected_methods_exist(self):
        """Test that all expected methods exist on the client."""
        expected_methods = [
            "article_publish",
            "article_create_draft",
        ]
        for expected_method in expected_methods:
            assert hasattr(
                ArticlesClient, expected_method
            ), f"Expected method '{expected_method}' not found on ArticlesClient"
            assert callable(
                getattr(ArticlesClient, expected_method)
            ), f"'{expected_method}' exists but is not callable"

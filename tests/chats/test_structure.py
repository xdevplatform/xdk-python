# AUTO-GENERATED FILE - DO NOT EDIT
# This file was automatically generated by the XDK build tool.
# Any manual changes will be overwritten on the next generation.
"""
Auto-generated structural tests for {"class_name": "Chats", "display_name": "chats", "import_name": "chats", "original": ["chats"], "property_name": "chats"} client.

This module contains tests that validate the structure and API surface
of the {"class_name": "Chats", "display_name": "chats", "import_name": "chats", "original": ["chats"], "property_name": "chats"} client. These tests ensure that all expected methods
exist, have correct signatures, and proper type annotations for robust API contracts.

Generated automatically - do not edit manually.
"""

import pytest
import inspect
from typing import get_type_hints
from xdk.chats.client import ChatsClient
from xdk import Client


class TestChatsStructure:
    """Test the structure of ChatsClient."""


    def setup_class(self):
        """Set up test fixtures."""
        # Provide all authentication types for comprehensive test coverage
        # Tests mock the session, so actual HTTP requests won't be made
        from xdk.oauth1_auth import OAuth1
        oauth1 = OAuth1(
            api_key="test_api_key",
            api_secret="test_api_secret",
            callback="http://localhost:8080/callback",
            access_token="test_access_token",
            access_token_secret="test_access_token_secret",
        )
        self.client = Client(
            base_url="https://api.example.com",
            bearer_token="test_bearer_token",
            access_token="test_access_token",
            auth=oauth1,
        )
        self.chats_client = getattr(self.client, "chats")


    def test_chat_media_upload_finalize_exists(self):
        """Test that chat_media_upload_finalize method exists with correct signature."""
        # Check method exists
        method = getattr(ChatsClient, "chat_media_upload_finalize", None)
        assert (
            method is not None
        ), f"Method chat_media_upload_finalize does not exist on ChatsClient"
        # Check method is callable
        assert callable(method), f"chat_media_upload_finalize is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert (
            len(params) >= 1
        ), f"chat_media_upload_finalize should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = [
            "id",
        ]
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from chat_media_upload_finalize"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = []
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_chat_media_upload_finalize_return_annotation(self):
        """Test that chat_media_upload_finalize has proper return type annotation."""
        method = getattr(ChatsClient, "chat_media_upload_finalize")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method chat_media_upload_finalize should have return type annotation"


    def test_add_chat_group_members_exists(self):
        """Test that add_chat_group_members method exists with correct signature."""
        # Check method exists
        method = getattr(ChatsClient, "add_chat_group_members", None)
        assert (
            method is not None
        ), f"Method add_chat_group_members does not exist on ChatsClient"
        # Check method is callable
        assert callable(method), f"add_chat_group_members is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert (
            len(params) >= 1
        ), f"add_chat_group_members should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = [
            "id",
        ]
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from add_chat_group_members"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = []
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_add_chat_group_members_return_annotation(self):
        """Test that add_chat_group_members has proper return type annotation."""
        method = getattr(ChatsClient, "add_chat_group_members")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method add_chat_group_members should have return type annotation"


    def test_get_users_public_keys_exists(self):
        """Test that get_users_public_keys method exists with correct signature."""
        # Check method exists
        method = getattr(ChatsClient, "get_users_public_keys", None)
        assert (
            method is not None
        ), f"Method get_users_public_keys does not exist on ChatsClient"
        # Check method is callable
        assert callable(method), f"get_users_public_keys is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert (
            len(params) >= 1
        ), f"get_users_public_keys should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = [
            "ids",
        ]
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from get_users_public_keys"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = [
            "public_key.fields",
        ]
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_get_users_public_keys_return_annotation(self):
        """Test that get_users_public_keys has proper return type annotation."""
        method = getattr(ChatsClient, "get_users_public_keys")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method get_users_public_keys should have return type annotation"


    def test_send_chat_message_exists(self):
        """Test that send_chat_message method exists with correct signature."""
        # Check method exists
        method = getattr(ChatsClient, "send_chat_message", None)
        assert (
            method is not None
        ), f"Method send_chat_message does not exist on ChatsClient"
        # Check method is callable
        assert callable(method), f"send_chat_message is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert (
            len(params) >= 1
        ), f"send_chat_message should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = [
            "id",
        ]
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from send_chat_message"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = []
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_send_chat_message_return_annotation(self):
        """Test that send_chat_message has proper return type annotation."""
        method = getattr(ChatsClient, "send_chat_message")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method send_chat_message should have return type annotation"


    def test_add_conversation_keys_exists(self):
        """Test that add_conversation_keys method exists with correct signature."""
        # Check method exists
        method = getattr(ChatsClient, "add_conversation_keys", None)
        assert (
            method is not None
        ), f"Method add_conversation_keys does not exist on ChatsClient"
        # Check method is callable
        assert callable(method), f"add_conversation_keys is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert (
            len(params) >= 1
        ), f"add_conversation_keys should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = [
            "id",
        ]
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from add_conversation_keys"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = []
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_add_conversation_keys_return_annotation(self):
        """Test that add_conversation_keys has proper return type annotation."""
        method = getattr(ChatsClient, "add_conversation_keys")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method add_conversation_keys should have return type annotation"


    def test_send_chat_typing_indicator_exists(self):
        """Test that send_chat_typing_indicator method exists with correct signature."""
        # Check method exists
        method = getattr(ChatsClient, "send_chat_typing_indicator", None)
        assert (
            method is not None
        ), f"Method send_chat_typing_indicator does not exist on ChatsClient"
        # Check method is callable
        assert callable(method), f"send_chat_typing_indicator is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert (
            len(params) >= 1
        ), f"send_chat_typing_indicator should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = [
            "id",
        ]
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from send_chat_typing_indicator"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = []
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_send_chat_typing_indicator_return_annotation(self):
        """Test that send_chat_typing_indicator has proper return type annotation."""
        method = getattr(ChatsClient, "send_chat_typing_indicator")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method send_chat_typing_indicator should have return type annotation"


    def test_get_chat_conversations_exists(self):
        """Test that get_chat_conversations method exists with correct signature."""
        # Check method exists
        method = getattr(ChatsClient, "get_chat_conversations", None)
        assert (
            method is not None
        ), f"Method get_chat_conversations does not exist on ChatsClient"
        # Check method is callable
        assert callable(method), f"get_chat_conversations is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert (
            len(params) >= 1
        ), f"get_chat_conversations should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = []
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from get_chat_conversations"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = [
            "max_results",
            "pagination_token",
            "chat_conversation.fields",
            "expansions",
            "user.fields",
        ]
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_get_chat_conversations_return_annotation(self):
        """Test that get_chat_conversations has proper return type annotation."""
        method = getattr(ChatsClient, "get_chat_conversations")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method get_chat_conversations should have return type annotation"


    def test_get_chat_conversations_pagination_params(self):
        """Test that get_chat_conversations has pagination parameters."""
        method = getattr(ChatsClient, "get_chat_conversations")
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have pagination-related parameters
        pagination_params = [
            "pagination_token",
            "max_results",
            "next_token",
            "cursor",
            "limit",
        ]
        has_pagination_param = any(param in params for param in pagination_params)
        assert (
            has_pagination_param
        ), f"Paginated method get_chat_conversations should have pagination parameters"


    def test_chat_media_upload_append_exists(self):
        """Test that chat_media_upload_append method exists with correct signature."""
        # Check method exists
        method = getattr(ChatsClient, "chat_media_upload_append", None)
        assert (
            method is not None
        ), f"Method chat_media_upload_append does not exist on ChatsClient"
        # Check method is callable
        assert callable(method), f"chat_media_upload_append is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert (
            len(params) >= 1
        ), f"chat_media_upload_append should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = [
            "id",
        ]
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from chat_media_upload_append"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = []
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_chat_media_upload_append_return_annotation(self):
        """Test that chat_media_upload_append has proper return type annotation."""
        method = getattr(ChatsClient, "chat_media_upload_append")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method chat_media_upload_append should have return type annotation"


    def test_get_chat_conversation_events_exists(self):
        """Test that get_chat_conversation_events method exists with correct signature."""
        # Check method exists
        method = getattr(ChatsClient, "get_chat_conversation_events", None)
        assert (
            method is not None
        ), f"Method get_chat_conversation_events does not exist on ChatsClient"
        # Check method is callable
        assert callable(method), f"get_chat_conversation_events is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert (
            len(params) >= 1
        ), f"get_chat_conversation_events should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = [
            "id",
        ]
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from get_chat_conversation_events"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = [
            "max_results",
            "pagination_token",
            "chat_message_event.fields",
        ]
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_get_chat_conversation_events_return_annotation(self):
        """Test that get_chat_conversation_events has proper return type annotation."""
        method = getattr(ChatsClient, "get_chat_conversation_events")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method get_chat_conversation_events should have return type annotation"


    def test_get_chat_conversation_events_pagination_params(self):
        """Test that get_chat_conversation_events has pagination parameters."""
        method = getattr(ChatsClient, "get_chat_conversation_events")
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have pagination-related parameters
        pagination_params = [
            "pagination_token",
            "max_results",
            "next_token",
            "cursor",
            "limit",
        ]
        has_pagination_param = any(param in params for param in pagination_params)
        assert (
            has_pagination_param
        ), f"Paginated method get_chat_conversation_events should have pagination parameters"


    def test_chat_media_download_exists(self):
        """Test that chat_media_download method exists with correct signature."""
        # Check method exists
        method = getattr(ChatsClient, "chat_media_download", None)
        assert (
            method is not None
        ), f"Method chat_media_download does not exist on ChatsClient"
        # Check method is callable
        assert callable(method), f"chat_media_download is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert (
            len(params) >= 1
        ), f"chat_media_download should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = [
            "id",
            "media_hash_key",
        ]
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from chat_media_download"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = []
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_chat_media_download_return_annotation(self):
        """Test that chat_media_download has proper return type annotation."""
        method = getattr(ChatsClient, "chat_media_download")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method chat_media_download should have return type annotation"


    def test_get_chat_conversation_exists(self):
        """Test that get_chat_conversation method exists with correct signature."""
        # Check method exists
        method = getattr(ChatsClient, "get_chat_conversation", None)
        assert (
            method is not None
        ), f"Method get_chat_conversation does not exist on ChatsClient"
        # Check method is callable
        assert callable(method), f"get_chat_conversation is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert (
            len(params) >= 1
        ), f"get_chat_conversation should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = [
            "id",
        ]
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from get_chat_conversation"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = [
            "chat_conversation.fields",
            "expansions",
            "user.fields",
        ]
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_get_chat_conversation_return_annotation(self):
        """Test that get_chat_conversation has proper return type annotation."""
        method = getattr(ChatsClient, "get_chat_conversation")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method get_chat_conversation should have return type annotation"


    def test_initialize_chat_group_exists(self):
        """Test that initialize_chat_group method exists with correct signature."""
        # Check method exists
        method = getattr(ChatsClient, "initialize_chat_group", None)
        assert (
            method is not None
        ), f"Method initialize_chat_group does not exist on ChatsClient"
        # Check method is callable
        assert callable(method), f"initialize_chat_group is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert (
            len(params) >= 1
        ), f"initialize_chat_group should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = []
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from initialize_chat_group"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = []
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_initialize_chat_group_return_annotation(self):
        """Test that initialize_chat_group has proper return type annotation."""
        method = getattr(ChatsClient, "initialize_chat_group")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method initialize_chat_group should have return type annotation"


    def test_chat_media_upload_initialize_exists(self):
        """Test that chat_media_upload_initialize method exists with correct signature."""
        # Check method exists
        method = getattr(ChatsClient, "chat_media_upload_initialize", None)
        assert (
            method is not None
        ), f"Method chat_media_upload_initialize does not exist on ChatsClient"
        # Check method is callable
        assert callable(method), f"chat_media_upload_initialize is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert (
            len(params) >= 1
        ), f"chat_media_upload_initialize should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = []
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from chat_media_upload_initialize"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = []
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_chat_media_upload_initialize_return_annotation(self):
        """Test that chat_media_upload_initialize has proper return type annotation."""
        method = getattr(ChatsClient, "chat_media_upload_initialize")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method chat_media_upload_initialize should have return type annotation"


    def test_delete_chat_messages_exists(self):
        """Test that delete_chat_messages method exists with correct signature."""
        # Check method exists
        method = getattr(ChatsClient, "delete_chat_messages", None)
        assert (
            method is not None
        ), f"Method delete_chat_messages does not exist on ChatsClient"
        # Check method is callable
        assert callable(method), f"delete_chat_messages is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert (
            len(params) >= 1
        ), f"delete_chat_messages should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = [
            "id",
        ]
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from delete_chat_messages"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = []
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_delete_chat_messages_return_annotation(self):
        """Test that delete_chat_messages has proper return type annotation."""
        method = getattr(ChatsClient, "delete_chat_messages")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method delete_chat_messages should have return type annotation"


    def test_get_users_public_key_exists(self):
        """Test that get_users_public_key method exists with correct signature."""
        # Check method exists
        method = getattr(ChatsClient, "get_users_public_key", None)
        assert (
            method is not None
        ), f"Method get_users_public_key does not exist on ChatsClient"
        # Check method is callable
        assert callable(method), f"get_users_public_key is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert (
            len(params) >= 1
        ), f"get_users_public_key should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = [
            "id",
        ]
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from get_users_public_key"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = [
            "public_key.fields",
        ]
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_get_users_public_key_return_annotation(self):
        """Test that get_users_public_key has proper return type annotation."""
        method = getattr(ChatsClient, "get_users_public_key")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method get_users_public_key should have return type annotation"


    def test_add_user_public_key_exists(self):
        """Test that add_user_public_key method exists with correct signature."""
        # Check method exists
        method = getattr(ChatsClient, "add_user_public_key", None)
        assert (
            method is not None
        ), f"Method add_user_public_key does not exist on ChatsClient"
        # Check method is callable
        assert callable(method), f"add_user_public_key is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert (
            len(params) >= 1
        ), f"add_user_public_key should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = [
            "id",
        ]
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from add_user_public_key"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = []
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_add_user_public_key_return_annotation(self):
        """Test that add_user_public_key has proper return type annotation."""
        method = getattr(ChatsClient, "add_user_public_key")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method add_user_public_key should have return type annotation"


    def test_create_chat_conversation_exists(self):
        """Test that create_chat_conversation method exists with correct signature."""
        # Check method exists
        method = getattr(ChatsClient, "create_chat_conversation", None)
        assert (
            method is not None
        ), f"Method create_chat_conversation does not exist on ChatsClient"
        # Check method is callable
        assert callable(method), f"create_chat_conversation is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert (
            len(params) >= 1
        ), f"create_chat_conversation should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = []
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from create_chat_conversation"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = []
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_create_chat_conversation_return_annotation(self):
        """Test that create_chat_conversation has proper return type annotation."""
        method = getattr(ChatsClient, "create_chat_conversation")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method create_chat_conversation should have return type annotation"


    def test_mark_chat_conversation_read_exists(self):
        """Test that mark_chat_conversation_read method exists with correct signature."""
        # Check method exists
        method = getattr(ChatsClient, "mark_chat_conversation_read", None)
        assert (
            method is not None
        ), f"Method mark_chat_conversation_read does not exist on ChatsClient"
        # Check method is callable
        assert callable(method), f"mark_chat_conversation_read is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert (
            len(params) >= 1
        ), f"mark_chat_conversation_read should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = [
            "id",
        ]
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from mark_chat_conversation_read"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = []
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_mark_chat_conversation_read_return_annotation(self):
        """Test that mark_chat_conversation_read has proper return type annotation."""
        method = getattr(ChatsClient, "mark_chat_conversation_read")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method mark_chat_conversation_read should have return type annotation"


    def test_all_expected_methods_exist(self):
        """Test that all expected methods exist on the client."""
        expected_methods = [
            "chat_media_upload_finalize",
            "add_chat_group_members",
            "get_users_public_keys",
            "send_chat_message",
            "add_conversation_keys",
            "send_chat_typing_indicator",
            "get_chat_conversations",
            "chat_media_upload_append",
            "get_chat_conversation_events",
            "chat_media_download",
            "get_chat_conversation",
            "initialize_chat_group",
            "chat_media_upload_initialize",
            "delete_chat_messages",
            "get_users_public_key",
            "add_user_public_key",
            "create_chat_conversation",
            "mark_chat_conversation_read",
        ]
        for expected_method in expected_methods:
            assert hasattr(
                ChatsClient, expected_method
            ), f"Expected method '{expected_method}' not found on ChatsClient"
            assert callable(
                getattr(ChatsClient, expected_method)
            ), f"'{expected_method}' exists but is not callable"

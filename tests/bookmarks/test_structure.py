# AUTO-GENERATED FILE - DO NOT EDIT
# This file was automatically generated by the XDK build tool.
# Any manual changes will be overwritten on the next generation.
"""
Auto-generated structural tests for {"class_name": "Bookmarks", "display_name": "bookmarks", "import_name": "bookmarks", "original": ["bookmarks"], "property_name": "bookmarks"} client.

This module contains tests that validate the structure and API surface
of the {"class_name": "Bookmarks", "display_name": "bookmarks", "import_name": "bookmarks", "original": ["bookmarks"], "property_name": "bookmarks"} client. These tests ensure that all expected methods
exist, have correct signatures, and proper type annotations for robust API contracts.

Generated automatically - do not edit manually.
"""

import pytest
import inspect
from typing import get_type_hints
from xdk.bookmarks.client import BookmarksClient
from xdk import Client


class TestBookmarksStructure:
    """Test the structure of BookmarksClient."""


    def setup_class(self):
        """Set up test fixtures."""
        # Provide all authentication types for comprehensive test coverage
        # Tests mock the session, so actual HTTP requests won't be made
        from xdk.oauth1_auth import OAuth1
        oauth1 = OAuth1(
            api_key="test_api_key",
            api_secret="test_api_secret",
            callback="http://localhost:8080/callback",
            access_token="test_access_token",
            access_token_secret="test_access_token_secret",
        )
        self.client = Client(
            base_url="https://api.example.com",
            bearer_token="test_bearer_token",
            access_token="test_access_token",
            auth=oauth1,
        )
        self.bookmarks_client = getattr(self.client, "bookmarks")


    def test_delete_users_bookmark_exists(self):
        """Test that delete_users_bookmark method exists with correct signature."""
        # Check method exists
        method = getattr(BookmarksClient, "delete_users_bookmark", None)
        assert (
            method is not None
        ), f"Method delete_users_bookmark does not exist on BookmarksClient"
        # Check method is callable
        assert callable(method), f"delete_users_bookmark is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert (
            len(params) >= 1
        ), f"delete_users_bookmark should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = [
            "id",
            "tweet_id",
        ]
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from delete_users_bookmark"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = []
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_delete_users_bookmark_return_annotation(self):
        """Test that delete_users_bookmark has proper return type annotation."""
        method = getattr(BookmarksClient, "delete_users_bookmark")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method delete_users_bookmark should have return type annotation"


    def test_create_users_bookmark_folder_exists(self):
        """Test that create_users_bookmark_folder method exists with correct signature."""
        # Check method exists
        method = getattr(BookmarksClient, "create_users_bookmark_folder", None)
        assert (
            method is not None
        ), f"Method create_users_bookmark_folder does not exist on BookmarksClient"
        # Check method is callable
        assert callable(method), f"create_users_bookmark_folder is not callable"
        # Check method signature
        sig = inspect.signature(method)
        params = list(sig.parameters.keys())
        # Should have 'self' as first parameter
        assert (
            len(params) >= 1
        ), f"create_users_bookmark_folder should have at least 'self' parameter"
        assert (
            params[0] == "self"
        ), f"First parameter should be 'self', got '{params[0]}'"
        # Check required parameters exist (excluding 'self')
        required_params = [
            "id",
        ]
        for required_param in required_params:
            assert (
                required_param in params
            ), f"Required parameter '{required_param}' missing from create_users_bookmark_folder"
        # Check optional parameters have defaults (excluding 'self')
        optional_params = []
        for optional_param in optional_params:
            if optional_param in params:
                param_obj = sig.parameters[optional_param]
                assert (
                    param_obj.default is not inspect.Parameter.empty
                ), f"Optional parameter '{optional_param}' should have a default value"


    def test_create_users_bookmark_folder_return_annotation(self):
        """Test that create_users_bookmark_folder has proper return type annotation."""
        method = getattr(BookmarksClient, "create_users_bookmark_folder")
        sig = inspect.signature(method)
        # Check return annotation exists
        assert (
            sig.return_annotation is not inspect.Signature.empty
        ), f"Method create_users_bookmark_folder should have return type annotation"


    def test_all_expected_methods_exist(self):
        """Test that all expected methods exist on the client."""
        expected_methods = [
            "delete_users_bookmark",
            "create_users_bookmark_folder",
        ]
        for expected_method in expected_methods:
            assert hasattr(
                BookmarksClient, expected_method
            ), f"Expected method '{expected_method}' not found on BookmarksClient"
            assert callable(
                getattr(BookmarksClient, expected_method)
            ), f"'{expected_method}' exists but is not callable"
